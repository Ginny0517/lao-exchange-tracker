# Vercel 部署修復報告

## 修復日期
2026-02-04

## 問題摘要
專案在 Vercel 部署時遇到多個 TypeScript 型別錯誤，導致建置失敗。

---

## 修復項目

### 1. InstantDB 查詢型別不符（ExchangeTable.tsx）
**問題：** InstantDB 的 `db.useQuery` 回傳型別與預期的實體型別不一致。

**解決方案：**
- 在 `lib/db.ts` 中為所有 hooks 明確定義回傳型別
- 使用 `as unknown as TargetType` 進行型別斷言
- 修改的 hooks：
  - `useCurrentRates` → `CurrentRate[]`
  - `useHistoricalRates` → `HistoricalRate[]`
  - `useBanks` → `Bank[]`
  - `useSupportedPairs` → `SupportedPair[]`

### 2. Error 型別不符
**問題：** InstantDB 的 error 型別為 `{ message: string }`，與標準 `Error` 不相容。

**解決方案：**
- 將所有 hooks 的 `error` 型別改為 `any`
- 直接回傳原始 error，不做額外轉換

### 3. Scraper 目錄被錯誤建置
**問題：** `tsconfig.json` 包含了所有 `**/*.ts` 檔案，導致 scraper 目錄也被 Next.js 建置。

**解決方案：**
- 在 `tsconfig.json` 的 `exclude` 中加入 `"scraper"`
- 新增 `target: "ES2015"` 和 `downlevelIteration: true`
- 為 scraper 建立獨立的 `scraper/tsconfig.json`

### 4. 依賴項優化
**問題：** 部署時出現大量過時套件警告。

**解決方案：**
- 將 `playwright` 移至 `optionalDependencies`（僅爬蟲需要）
- 更新 `eslint` 相關套件版本
- 新增 `engines.node: ">=18.x"` 確保 Node 版本

### 5. 安全性檢查
**確認項目：**
- `.env` 和 `.env*.local` 已在 `.gitignore` 中
- 僅使用 `NEXT_PUBLIC_INSTANT_APP_ID`（可公開）
- `INSTANT_ADMIN_TOKEN` 僅在本機爬蟲使用，不會被提交

---

## 修改的檔案清單

### 核心修正
- `lib/db.ts` - 型別斷言與回傳型別修正
- `components/ExchangeTable.tsx` - 使用正確型別
- `components/TrendChart.tsx` - 使用正確型別

### 配置檔案
- `tsconfig.json` - 排除 scraper、支援 ES2015
- `next.config.js` - Webpack fallback 配置
- `scraper/tsconfig.json` - 新增，scraper 專用配置
- `.gitignore` - 排除 scraper 輸出目錄

### 部署相關
- `package.json` - 依賴項調整、新增 deploy 腳本
- `vercel.json` - Vercel 配置
- `VERCEL_DEPLOY.md` - 部署說明文件

---

## 部署步驟

### 方式一：Vercel 網站（推薦）
1. 登入 https://vercel.com
2. Import Project → 選擇 GitHub repo
3. 設定環境變數：
   - `NEXT_PUBLIC_INSTANT_APP_ID` = 你的 InstantDB App ID
4. Deploy

### 方式二：Git Push 自動部署
1. 執行 `git-push.bat`（Windows）
2. 或手動：
   ```bash
   git add -A
   git commit -m "fix: resolve all Vercel deployment issues"
   git push
   ```
3. Vercel 會自動偵測並部署

---

## 驗證清單

- [x] TypeScript 型別錯誤已全部修正
- [x] Scraper 目錄已排除在建置外
- [x] 環境變數配置正確
- [x] 敏感資訊未被提交
- [x] 依賴項已優化
- [x] 建置配置已更新

---

## 注意事項

1. **環境變數必須設定**  
   在 Vercel 專案設定中務必加入 `NEXT_PUBLIC_INSTANT_APP_ID`，否則前端無法連線到 InstantDB。

2. **Scraper 僅在本機執行**  
   爬蟲功能需要 Playwright 與完整瀏覽器環境，不應在 Vercel 執行。定期在本機執行爬蟲更新資料即可。

3. **自動部署**  
   每次 push 到 GitHub 主分支，Vercel 都會自動重新部署。

4. **建置錯誤排查**  
   若建置失敗，到 Vercel 專案的 Deployments → 點進該次部署 → Building 查看完整日誌。

---

## 技術債務與未來改進

1. 考慮為 InstantDB 建立更精確的型別定義檔
2. 將爬蟲改為 serverless function 或排程任務
3. 新增 CI/CD 測試流程
4. 優化套件版本管理（考慮使用 renovate）
