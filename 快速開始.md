# 🚀 快速開始指南

## 第一步：安裝依賴

在 `web` 目錄下打開命令提示符（cmd）或終端，運行：

```bash
npm install
```

或者雙擊 `install.bat` 文件自動安裝。

## 第二步：配置 InstantDB（可選）

1. 前往 [https://instantdb.com](https://instantdb.com) 註冊帳號
2. 創建新的應用，獲取 App ID
3. 在 `web` 目錄下創建 `.env.local` 文件：

```
NEXT_PUBLIC_INSTANT_APP_ID=your_app_id_here
```

**注意**：當前已包含模擬數據，即使不配置 InstantDB 也可以運行查看 UI。

## 第三步：啟動開發服務器

```bash
npm run dev
```

或者雙擊 `start.bat` 文件。

## 第四步：查看應用

打開瀏覽器訪問：[http://localhost:3000](http://localhost:3000)

## 📁 項目結構

```
web/
├── app/                    # Next.js 14 頁面
│   ├── layout.tsx         # 全局佈局
│   ├── page.tsx           # 主頁（整合所有組件）
│   └── globals.css        # 全局樣式
├── components/            # React 組件
│   ├── Header.tsx         # 頁頭（導航、語言、深色模式）
│   ├── FilterBar.tsx      # 篩選欄（匯率對、銀行選擇）
│   ├── ExchangeTable.tsx  # 匯率表格（可排序）
│   ├── TrendChart.tsx     # 7日走勢圖（Recharts）
│   └── StatsCard.tsx      # 統計卡片
├── lib/                   # 核心邏輯
│   ├── db.ts             # InstantDB 連接
│   ├── schema.ts         # 數據模型
│   ├── types.ts          # TypeScript 類型
│   └── mockData.ts       # 模擬數據（開發用）
└── package.json          # 項目配置
```

## ✨ 功能特點

✅ **實時匯率對比** - USD、THB、CNY、EUR 對 LAK  
✅ **多銀行支持** - BOL、BCEL、LDB、APB、LVB  
✅ **7日走勢圖** - Recharts 視覺化  
✅ **智能篩選** - 匯率對選擇、銀行篩選  
✅ **深色模式** - 自動跟隨系統偏好  
✅ **多語言** - 繁中、英文、寮文  
✅ **響應式設計** - Mobile-first，完美支持手機  
✅ **最佳匯率標記** - 自動高亮最優價格  

## 🎨 設計系統

- **主色調**: #2196F3（藍色）
- **強調色**: #00BCD4（青色）
- **成功色**: #4CAF50（綠色）
- **深色模式**: 完整支持
- **無障礙**: ARIA 標籤、鍵盤導航

## 🔧 下一步

1. **連接真實數據源**：將 Python 後端的匯率數據同步到 InstantDB
2. **添加更多銀行**：編輯 `lib/mockData.ts`
3. **添加更多幣種**：更新 supportedPairs
4. **自定義樣式**：修改 `tailwind.config.ts`
5. **部署上線**：使用 Vercel 一鍵部署

## 📝 常見問題

**Q: 如何添加新銀行？**  
A: 編輯 `lib/mockData.ts` 中的 `mockBanks` 數組

**Q: 如何修改顏色主題？**  
A: 編輯 `tailwind.config.ts` 中的 colors 配置

**Q: 如何連接真實數據？**  
A: 配置 InstantDB，然後移除組件中對 mockData 的引用，改用 `lib/db.ts` 中的 hooks

## 🆘 需要幫助？

- 查看 `README.md` 了解更多詳情
- InstantDB 文檔：https://instantdb.com/docs
- Next.js 文檔：https://nextjs.org/docs
- Recharts 文檔：https://recharts.org

---

**祝開發順利！** 🎉
