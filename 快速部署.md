# 快速部署指南

## 立即執行

### 步驟 1: Git Push（必須）
在專案資料夾雙擊執行：
```
git-push.bat
```

或手動執行：
```bash
git add -A
git commit -m "fix: resolve all Vercel deployment issues"
git push
```

### 步驟 2: Vercel 部署
選擇以下任一方式：

#### 方式 A：自動部署（推薦）
如果已連結 GitHub，push 後 Vercel 會自動部署。
前往 https://vercel.com 查看部署狀態。

#### 方式 B：手動匯入
1. 登入 https://vercel.com
2. New Project → Import Git Repository
3. 選擇 `Ginny0517/lao-exchange-tracker`
4. 設定環境變數：
   - Name: `NEXT_PUBLIC_INSTANT_APP_ID`
   - Value: (從 `.env.local` 複製)
5. Deploy

---

## 本次修復內容

✅ 修正所有 TypeScript 型別錯誤  
✅ 排除 scraper 目錄不被建置  
✅ 優化依賴項配置  
✅ 更新建置配置支援 ES2015+  
✅ 確認安全性無洩漏  

詳細內容見：`部署修復報告.md`

---

## 環境變數（重要）

Vercel 必須設定：
- `NEXT_PUBLIC_INSTANT_APP_ID`

本機開發使用 `.env.local`：
```env
NEXT_PUBLIC_INSTANT_APP_ID=your_app_id_here
INSTANT_ADMIN_TOKEN=your_admin_token_here
```

---

## 常見問題

**Q: 建置失敗怎麼辦？**  
A: 到 Vercel → Deployments → 點進失敗的那次 → Building 查看完整錯誤訊息。

**Q: 環境變數在哪設定？**  
A: Vercel 專案 → Settings → Environment Variables

**Q: 如何更新匯率資料？**  
A: 在本機執行 `npm run scrape:run`（需先安裝 playwright）

---

## 驗證部署成功

部署完成後，訪問 Vercel 給的網址，應該看到：
- 匯率表格正常顯示
- 無紅字錯誤
- 可篩選幣別與銀行
- 深色模式切換正常

若資料是空的：執行本機爬蟲更新 InstantDB 資料。
